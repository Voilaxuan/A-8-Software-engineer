# check.py
def perform_code_check(filepath, filename):
    # 在这里执行代码错误检查的操作
    # 使用传入的文件路径和名称来读取代码文件并进行检查

    # 假设检查结果存储在一个字符串中
    result = "No errors found"

    return result

def perform_security_check(filepath, filename):
    # 在这里执行代码错误检查的操作
    # 使用传入的文件路径和名称来读取代码文件并进行检查

    # 假设检查结果存储在一个字符串中
    result = "No errors found"

    return result
